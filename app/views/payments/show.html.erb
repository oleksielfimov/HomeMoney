<h1>Done</h1>

<p>
  <strong>Account:</strong>
   <%= @payment.account %>
</p>
 
<p>
  <strong>Ballance:</strong>
  <%= @payment.ballance %>
</p>

<%= button_to 'Back', { :action => :new }, :method => :get %>
<%= button_to 'View all accounts', { :action => :index }, :method => :get %>
<%= button_to 'Delete', { :action => :destroy, :id => @payment.id }, :method => :delete, :confirm => 'Честно?' %>